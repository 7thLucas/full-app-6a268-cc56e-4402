/**
 * Re-export from src/model for auto-discovery.
 * The model scanner checks the module root for *.model.ts files.
 */
export { InterviewSession, InterviewSessionModel } from "./src/model/audio-analyzer.model";
